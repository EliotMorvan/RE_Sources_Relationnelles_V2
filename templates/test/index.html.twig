{# templates/test/index.html.twig #}

{% extends "base.html.twig" %}

{% block body %}
<div class="container">
    <p>
        {{ my_variable }}
    </p>
    <p>
        {{ my_user.name }} - {{ my_user.email }}
    </p>
    <p>
        BOB - {{ bob_user.email }}
    </p>
    <p>
        <a href="{{ path('test_foo')}}">Page Foo</a>
    </p>
</div>
{% endblock %}